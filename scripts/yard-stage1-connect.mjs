/**
 * Stage 1 connectivity check — run via:
 *   npx --yes tsx --tsconfig tsconfig.json scripts/yard-stage1-connect.ts
 * This mjs wrapper stays for `npm test` discovery; the real harness is the .ts file.
 */
console.log("use: npx tsx --tsconfig tsconfig.json scripts/yard-stage1-connect.ts");
