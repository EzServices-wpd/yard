# Bot — read this first

Yard is house-first. **Type it. Buy the parts. Build it.** You write voice, not SKUs.

Full briefing (vision + queue + worked examples): [`docs/BOT-BRIEF.md`](BOT-BRIEF.md).

## First command (every fire)

```
npx tsx --tsconfig tsconfig.json scripts/walk-canary.ts
```

- **Green** → one item from NEXT below. Ship full files. Walk canary again.
- **Red** → fix *that* canary. Stop. Do not invent a FAIL class. Do not open `fitted.ts`.

Then `scripts/walk-launch.ts` should be 10/10. Linen name is **Linen**, not Closet.

## NEXT (one at a time)

1. **Pocket stranger PDF** — freeze #1 routing is fixed; the printed plan is not yet linen-quality.
2. **Slice D** — real alcove photo for landing heroes. Current jpgs are bench renders. Ask if you do not have photographs.

**Shipped:** Cut A (20 Sep queue / 21 Sep) — 0.25" back `materialId` is `plywood-1-4-4x8`. Thin stays off the ¾" nest. Linen 31.5 × 78 × 16 unchanged.

## Parked

- Lounge / desk / AABB catalog width
- New form classes
- Hourly overnight bot
- Amazon tag / accounts
- PLACEHOLDER files

## Never

- Invent a size the prompt did not give
- Push empty / PLACEHOLDER files
- Wipe canvas, stick-cloud, steps, or nest-plate
- Restart the hourly refiner
- Treat “chair space” as a chair — it is knee room (freeze #1 pocket)

## Freeze

| # | Must stay |
|---|---|
| Linen | **31.5 × 78 × 16**, kind closet, name Linen |
| Pocket | original survey → vanity + `project.pocket`, never House |
